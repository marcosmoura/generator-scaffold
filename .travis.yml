language: node_js
node_js:
  - '0.10'
before_install:
  - currentfolder=${PWD##*/}
  - if [ "$currentfolder" != 'generator-scaffold' ]; then cd .. && eval "mv $currentfolder generator-scaffold" && cd generator-scaffold; fi

